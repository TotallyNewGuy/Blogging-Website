spring:
  zipkin:
    base-url: http://127.0.0.1:9411/
    sender:
      type: web

  sleuth:
    sampler:
      probability: 1

  cloud:
    config:
#      uri: http://localhost:6000
      name: gateway
      profile: dev
      label: main
      discovery:
        enabled: true
        service-id: config-serive
      fail-fast: true
