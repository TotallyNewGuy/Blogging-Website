spring:
  cloud:
    config:
#      uri: http://localhost:6000
      name: article
      profile: dev
      label: main
      discovery:
        enabled: true
        service-id: config-serive
      fail-fast: true